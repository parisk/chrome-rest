# Chrome REST

Manipulate Chrome through a REST API.

## API calls

### List tabs

`GET /api/tabs`

### Create a new tab

`POST /api/tabs`

### Get tab info

`GET /api/tabs/:tabid`

### Update a tab

`PUT /api/tabs/:tabid`

### Close a tab

`DELETE /api/tabs/:tabid`
